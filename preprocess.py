from pydantic import BaseModel
def preprocess_feature_data (data):
   tutoring = {
      "Yes":1.0,
      "No":0.0
   }
   ParentalSupport = {
      
   }
   
def loadInputIterative(data):
   simpan = []
   for value in data:
      simpan.append(value)
   return simpan
 